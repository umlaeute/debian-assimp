Author: IOhannes m zmölnig
Description: prevent use of printf with 'format not a string literal and no
format arguments' (fails to compile with "-Werror=format-security")
--- assimp.orig/tools/assimp_cmd/WriteDumb.cpp
+++ assimp/tools/assimp_cmd/WriteDumb.cpp
@@ -1150,7 +1150,7 @@
 	const char* fail = "assimp dump: Invalid number of arguments. "
 			"See \'assimp dump --help\'\r\n";
 	if (num < 1) {
-		printf(fail);
+		printf("%s", fail);
 		return 1;
 	}
 
@@ -1162,7 +1162,7 @@
 
 	// asssimp dump in out [options]
 	if (num < 1) {
-		printf(fail);
+		printf("%s", fail);
 		return 1;
 	}
 
